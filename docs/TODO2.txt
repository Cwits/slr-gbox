что сделать - 
    n. перевести projectview и unitview на shared_ptr, и изменить их обновление на ptr = make_shared(data),
        что бы ui пулил новые данные всегда
        ui должен хранить weak_ptr<> и при обновлении делать lock()
    n. переименовать modules в unit -> всё будет unit
    n. переименовать events в actions?

сделать раздельную загрузку ассетов и клипов.
если закидывается файл в unitUI то в начале проверяется можно ли загрузить как ассет, а потом только пробует загрузиться как клип


1. надо новые события - openFile(path), loadAsAsset, loadAsClip, 

    соотв у AudioUnit новые функции - assetLoaded(asset);??
    или как лучше это сделать то... если пресет то надо выставлять параметры в нужные значения. 
        Т.к. все параметры хранятся в _flatParameterList то можно делать снэпшот списка конкретного плагина, 
        применять новые параметры и скармливать в РТ это всё дело.
        условно
        ParameterSnapshot snap = makeSnapshot(unit);
        snap->applyPreset(preset);

        FlatEvents...
        ev.type = assetLoaded;
        ev.assetLoaded.assetType = preset;
        ev.assetLoaded.preset = snap.get(); 
        toRt(ev);

        //in rt
        unit->assetLoaded(ev.assetLoaded);
        bool assetLoaded(assetStruct &ass) {
            switch(ass.type) {
                case(preset): {
                    for(ass.numParams) {
                        _flatParameterList[i] = ass.par[i];
                    }
                    return true;
                } break;
                case(audiofile): {
                    //whatever you need to do with this file
                    return false;
                } break;
            }
        }

        но для этого надо новую систему событий, где генератор делает целую структуру под конкретное событие 
        и эти события живут пока не выполнятся до конца(пока не пройдет последний этап)...